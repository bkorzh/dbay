build/
dist/


backend/file.log

# The whole compiled frontend is a build artifact: `build.sh frontend` writes it,
# and index.html references content-hashed asset filenames, so a committed copy
# goes stale on any code change and dirties the tree after every build.
backend/compiled_frontend/