This module is used to control the other modules in a rack. It contains a microcontroller which uses an Arduino firmware. The module acts as the sole connection between the outside world and the rack. It can be connected to a computer via USB Type B and to a local network via Ethernet.

Further information:
- [[Install the Control Module]] 